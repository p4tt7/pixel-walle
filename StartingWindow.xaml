<Window x:Class="pixel_walle.StartingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Definir Dimensiones" 
        Height="768" 
        Width="1366"
        Background="White">

    <Grid>
        <Image Source="C:\Users\david\OneDrive\Pictures\logo2.png"
               HorizontalAlignment="Center"
               VerticalAlignment="Top"
               Height="348"
               Width="366"
               Margin="0,20,0,0" />

        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,387,0,0" >
            <Button Content="NEW PROJECT"
                    Click="New_Project"
                    Background="White"
                    BorderBrush="Black"
                    BorderThickness="3"
                    FontSize="25"
                    FontWeight="Bold"
                    Foreground="Black"
                    Width="506"
                    Height="60"
                    Cursor="Hand"/>

            <Button Content="LOAD PROJECT"
                    Click="Load_Project"
                    Background="White"
                    BorderBrush="Black"
                    BorderThickness="3"
                    FontSize="25"
                    FontWeight="Bold"
                    Foreground="Black"
                    Width="506"
                    Height="60"
                    Cursor="Hand"/>

            <Button Content="EXIT"
                    Click="ExitButton_Click"
                    Background="LightGray"
                    BorderBrush="Black"
                    BorderThickness="3"
                    FontSize="25"
                    FontWeight="Bold"
                    Foreground="Black"
                    Width="506"
                    Height="60"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>

