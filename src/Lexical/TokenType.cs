namespace pixel_walle.src.Lexical
{
        public enum TokenType
        {
            Unknown,
            Number,
            Text,
            Color,
            Keyword,
            Identifier,
            Symbol,
            EOF, 
            Newline

        }
}
