<Window x:Class="pixel_walle.StartingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Definir Dimensiones" 
        Height="540" 
        Width="960"
        Background="White">

    <Grid>
        <Image Source="C:\Users\david\OneDrive\Pictures\logo2.png"
               HorizontalAlignment="Left"
               VerticalAlignment="Top"
               Height="259"
               Width="222"
               Margin="350,59,0,0" />

        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Margin="230,286,0,0" >

            <Button Content="NEW PROJECT"
                    Click="New_Project"
                    Background="White"
                    BorderBrush="Black"
                    BorderThickness="3"
                    FontSize="15"
                    FontWeight="Bold"
                    Foreground="Black"
                    Width="463"
                    Height="50"
                    Cursor="Hand"/>

            <Button Content="LOAD PROJECT"
                    Click="Load_Project"
                    Background="White"
                    BorderBrush="Black"
                    BorderThickness="3"
                    FontSize="15"
                    FontWeight="Bold"
                    Foreground="Black"
                    Width="463"
                    Height="51"
                    Cursor="Hand"/>

            <Button Content="EXIT"
                    Click="ExitButton_Click"
                    Background="LightGray"
                    BorderBrush="Black"
                    BorderThickness="3"
                    FontSize="15"
                    FontWeight="Bold"
                    Foreground="Black"
                    Width="463"
                    Height="50"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>

